syntax = "proto3";
package generator;
option go_package = "./proto";

service IDService {
  rpc GetNextID (IDRequest) returns (IDResponse);
}

message IDRequest {}
message IDResponse {
  int64 id = 1;
  string code = 2;
}
